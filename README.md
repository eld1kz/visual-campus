# Visual Campus

**LOCUS Hackathon · Case 1 «Visual Campus»**

## Задача

Абитуриент хочет понять, как на самом деле выглядят кампус и студенческая жизнь вуза, но фотографии разбросаны по разным сайтам, часто без источника и с чужими местами. Нужно за ≤30 секунд по названию университета собрать **проверенный визуальный профиль**: фото по категориям, у каждого — источник, лицензия и уровень достоверности.

## Решение

1. **Распознавание** — пользователь вводит название (с опечатками, на любом языке). Бэкенд параллельно ищет в ROR и Wikidata, объединяет кандидатов и либо однозначно определяет вуз, либо предлагает выбрать.
2. **Сбор и проверка фото** — Wikimedia Commons, Wikidata, OpenStreetMap, Openverse, Mapillary, Flickr и официальный сайт; для каждого фото считается шкала достоверности по доказательствам: геотег внутри контура кампуса, категория Commons, упоминания в описании и локальная OpenCLIP-проверка сцены.
3. **Профиль** — фото по категориям, карта кампуса (2D / 3D), гид-маскот «Кампи», который отвечает только по найденным источникам.

## Стек

| Часть | Технологии |
|---|---|
| Backend | Python 3.12, FastAPI, httpx (asyncio), Pydantic v2, Shapely, imagehash, OpenCLIP + PyTorch |
| Frontend | Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS v4 |
| Дизайн | Исходные макеты и токены в `design/` |

## Структура репозитория

```
visual-campus/
├── design/                 исходные макеты (.dc.html) и токены оформления
├── backend/
│   ├── app/
│   │   ├── main.py         FastAPI, CORS, логирование времени запросов
│   │   ├── config.py       настройки из .env
│   │   ├── models.py       Pydantic-модели ответов
│   │   ├── routers/        /health, /resolve, /profile (SSE), /campus, /chat, /ai
│   │   └── services/       источники, план запросов, оркестратор, скоринг, дедупликация и vision
│   ├── requirements.txt
│   └── .env.example
├── frontend/
│   ├── app/                страницы: / (поиск, реальный API), /collecting, /profile, /compare, /guide, /wardrobe, /unavailable
│   ├── components/         search, profile, photos, map, chat, guide, mascot, wardrobe, compare, ui, layout
│   ├── lib/
│   │   ├── api.ts          клиент бэкенда
│   │   ├── types.ts        типы данных (docs/CONTRACT.md)
│   │   ├── i18n.ts         тексты RU/EN
│   │   └── mock/           демо-данные для экранов, ещё не подключённых к API
│   └── .env.local.example
└── README.md
```

## Запуск локально

### Backend

```bash
cd backend
python3.12 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env          # ключи пока не нужны для /resolve
# Один раз до демо: загрузить ~577 MB весов локального OpenCLIP, чтобы не тратить первый запрос на download.
python -c "import open_clip; open_clip.create_model_and_transforms('ViT-B-32', pretrained='laion2b_s34b_b79k')"
uvicorn app.main:app --reload --port 8000
```

Проверка:

```bash
curl localhost:8000/health
curl "localhost:8000/resolve?q=Korea%20University"
curl "localhost:8000/profile/Q39997?lang=ru"
```

Документация API: http://localhost:8000/docs

Тесты (без сети):

```bash
pip install -r requirements-dev.txt
pytest
```

### Frontend

```bash
cd frontend
npm install
cp .env.local.example .env.local   # NEXT_PUBLIC_API_URL=http://localhost:8000
npm run dev                        # http://localhost:3000
```

## API

### `GET /resolve?q=<название>`

Параллельно (asyncio + httpx, таймаут 5 с на источник) опрашивает:

- **ROR API v2** — сначала affiliation-матчер (устойчив к «грязным» названиям), затем обычный поиск для аббревиатур вроде `KAIST`;
- **Wikidata** — `wbsearchentities` → `wbgetentities` (координаты P625, сайт P856, категория Commons P373, ROR ID P6782). Если ничего не найдено, берётся исправление опечатки из поиска Wikipedia (`Koera Univrsity` → `korea university`), и ROR повторно опрашивается уже с исправленным запросом.

Кандидаты объединяются по ROR ID / Wikidata QID, иначе по совпадению названия и расстоянию ≤15 км. `match_score` = похожесть названия (с учётом алиасов и опечаток) + согласие двух источников + известность (число sitelinks в Wikidata).

| status | когда |
|---|---|
| `resolved` | лучший кандидат почти точно совпал по названию и заметно опережает остальных; запрос не является просто названием города |
| `ambiguous` | до 5 кандидатов на выбор |
| `not_found` | нет похожих кандидатов |

Если один источник упал или не ответил, ответ всё равно приходит, а в `sources_status` будет `timeout` или `error`. Если недоступны оба источника, возвращается `503`.

### `GET /profile/{wikidata_id}?lang=ru|en`

Проверенный визуальный профиль по `wikidata_id` из `/resolve` (например, `Q39997` — Korea University). Формат ответа соответствует контракту из `docs/CONTRACT.md`, плюс `stats` и координаты фото для карты.

1. **Wikidata** — названия на всех языках, координаты, категория Commons, статьи Wikipedia, центр города (штаб-квартира / «расположен в» → ближайший объект-город).
2. Параллельно, у каждого источника свой таймаут:
   - **OpenStreetMap** — контур кампуса: Nominatim по тегу `wikidata` (~1 с), при неудаче Overpass;
   - **Wikimedia Commons** — категории вуза, свежие годовые подкатегории, `depicts` P180, локальные названия и геопоиск, покрывающий весь полигон кампуса;
   - **Flickr** — сначала CC-фото, снятые с 2024 года, newest-first в bbox кампуса; затем более старые при дефиците (`FLICKR_API_KEY`);
   - **Mapillary** — сначала снимки `captured_at >= 2024`, затем старые при дефиците (`MAPILLARY_TOKEN`);
   - **Openverse** — дополнительное CC/PD-покрытие; анонимно до 20 результатов на запрос и ~1 запрос/с (page_size > 20 отклоняется с 401); запрос «‹название› campus», при < 10 результатах — просто название; client credentials повышают лимиты;
   - **Официальный сайт** — `og:image`, релевантные `img alt` и свежие страницы из sitemap; `lastmod` используется только для выбора страницы;
   - **Brave Image Search** — запасной веб-поиск свежих страниц (`BRAVE_SEARCH_API_KEY`), не считается проверенным сам по себе;
   - **Wikipedia** — краткое описание со ссылкой.
3. **Проверка каждого фото** (`backend/app/services/pipeline/scoring.py`). Уверенность = 40 + сумма весов доказательств, ограничена 0–100:

| Доказательство | Вес |
|---|---|
| Геотег внутри контура кампуса | +28 |
| Геотег ≤500 м от точки вуза (если контура нет) | +20 |
| Геотег ≤1 км за границей кампуса | +6 |
| Геотег в 1–3 км / дальше 3 км | −10 / −30 |
| Нет геотега | −6 |
| В категории Commons вуза / в подкатегории | +22 / +16 |
| Название вуза в названии или описании файла | +14 |
| Похоже на фото людей, мероприятия, еды крупным планом или объекта, а не места | −15 |
| Визуальная проверка OpenCLIP: похоже на место / не похоже / неубедительно | +12 / −20 / 0 |
| Лицензия не указана | −12 |
| Дата съёмки / только дата загрузки | evidence с весом 0; свежесть не меняет достоверность |

   Уровни: **Проверено** ≥ 80, **Вероятно** ≥ 60, иначе **Не подтверждено**. Непроверенное визуально фото ограничено уровнем `likely`. Отрицательный вердикт модели — мягкий штраф, который перекрывают геотег в контуре, категория Commons и название в описании; только уверенный вердикт с широким отрывом держит фото на «Не подтверждено». Даты съёмки и загрузки хранятся отдельно; upload/index/page dates не выдаются за дату съёмки. Порядок показа: надёжные фото по достоверности с бонусом за свежесть (2024+: +20, 2020–2023: +10, до 2020: −5, исторические: −10), поэтому свежее «Вероятно» стоит выше «Проверено» 2011 года; неподтверждённые всегда в конце. Источники сначала запрашивают свежее: Flickr и Mapillary с 2024 года, категории Commons — новые файлы первыми. Исторические фото скрыты отдельным переключателем.
4. Результат кэшируется в памяти на 30 минут.

Предыдущая проверка на живых данных (без ключей Flickr/Mapillary; условия, источники и spot-check — в [`docs/EVAL.md`](docs/EVAL.md)). Неподтверждённые фото теперь тоже отдаются и скрыты в интерфейсе за переключателем:

| Вуз | Время | Кандидатов | Проверено / вероятно / не подтверждено | Надёжных 2020+ |
|---|---|---|---|---|
| Nazarbayev University (`Q2783344`) | 20.3 с | 163 | 4 / 7 / 114 | 0 |
| KAIST (`Q39949`) | 13.3 с | 80 | 19 / 0 / 24 | 3 |
| Al-Farabi KazNU (`Q427677`) | 14.9 с | 138 | 3 / 3 / 114 | 2 |
| Korea University (`Q39997`) | 12.9 с | 113 | 5 / 11 / 70 | 3 |
| University of Cambridge (`Q35794`) | 8.9 с | 20 | 0 / 0 / 14 | 0 |

`404` — нет такого элемента Wikidata; `503` — Wikidata недоступна. Если OSM, Commons или Wikipedia не ответили, профиль всё равно собирается, а статус виден в `sources_status`.

## Готовые компоненты, AI и внешние API

- **ROR (Research Organization Registry)** — https://ror.org, API v2: поиск организаций, ROR ID, города, сайты.
- **Wikidata** — https://www.wikidata.org: сущности вузов, координаты, категории Commons, связка с ROR.
- **Wikipedia** — Search API (исправление опечаток) и REST API (краткое описание кампуса).
- **Wikimedia Commons API** — фотографии, авторы, лицензии, даты, геотеги, SHA-1.
- **Flickr API** — CC-фото по bbox, сначала снятые с 2024 года (нужен ключ).
- **Mapillary Graph API** — уличные снимки по bbox и `captured_at` (нужен токен).
- **Openverse API** — дополнительный поиск CC/PD-фото; анонимный режим ограничен 20 результатами на запрос.
- **OpenStreetMap** — Nominatim и Overpass API: контур кампуса (данные © OpenStreetMap contributors, ODbL).
- **Shapely** — проверка «геотег внутри контура кампуса».
- **OpenCLIP + PyTorch** — локальная пакетная проверка, что изображение показывает место, а не логотип, документ, мероприятие или объект.
- **Anthropic API** — дополнительная визуальная проверка и чат: `claude-haiku-4-5`; описание: `claude-opus-5`. Нужен `LLM_API_KEY`. При ошибках описание возвращается к выдержке Wikipedia, фотографии сохраняют результаты локальной проверки. Чат может использовать встроенный веб-поиск Anthropic со ссылками; дневные бюджеты задаются в `.env`.
- **Brave Image Search API** — дополнительный поиск фотографий, `BRAVE_SEARCH_API_KEY`; источник поиска сам по себе не подтверждает принадлежность фото и не предоставляет лицензию.
- **Готовые компоненты** — Next.js / React, Tailwind CSS, FastAPI / Pydantic, httpx, Pillow / imagehash и MapLibre GL JS. Карта использует OpenFreeMap, маршруты — публичный OSRM. Данные карты — OpenStreetMap. Версии зависимостей: `frontend/package-lock.json`, `backend/requirements.txt`.
- **Шрифты** — Instrument Sans и JetBrains Mono (Google Fonts через `next/font`).

Точный формат полей, дат, evidence и SSE-событий описан в [`docs/CONTRACT.md`](docs/CONTRACT.md).

## Ограничения

- Без `FLICKR_API_KEY` и `MAPILLARY_TOKEN` соответствующие источники честно получают статус `skipped`; Openverse продолжает работать анонимно с меньшим лимитом.
- Официальные сайты могут отвечать 403. Их изображения имеют неизвестную лицензию, показываются только ссылкой на источник и проходят визуальную проверку, если сам файл доступен.
- Если надёжных фото для категории нет, профиль показывает «Недостаточно данных» и список проверенных источников; `unconfirmed` фото при этом всё равно передаются и доступны за переключателем «Показать неподтверждённые».
- Публичный Overpass бывает медленным и ограничивает частоту запросов; если он не ответил, профиль строится без контура кампуса (проверка по расстоянию до точки вуза).
- Даты загрузки, индексации и `sitemap lastmod` не выдаются за дату съёмки. У многих старых Commons-файлов дата известна, а у свежих фото метаданные часто отсутствуют; тогда UI пишет `date unknown` или `uploaded …`.
- Веса доказательств и ключевые слова категорий подобраны вручную и проверены на ограниченном наборе вузов. Результаты ручной проверки записаны в [`docs/EVAL.md`](docs/EVAL.md).
- Карта и 3D-вид на странице профиля построены на MapLibre GL JS. Демонстрационная карта использует отдельные CSS-компоненты.
- В макете у поиска есть автоподсказки, но пока их нет: `/resolve` отвечает 2–4 с, для подсказок при вводе нужен отдельный быстрый эндпоинт.
- Классификация resolved/ambiguous подобрана эвристически и проверена на ограниченном наборе запросов.


## Проверочный сценарий для жюри

1. На главной ввести `Korea University`, проверить город и нажать «Собрать профиль».
2. Переключить категории и фильтры «общежитие», «спорт», «лаборатории», «студенческая жизнь»; открыть фото и проверить источник, дату, лицензию и доказательства.
3. Проверить сортировку по дате, карту 2D/3D и предупреждения о недостатке данных.
4. Повторить с новым вузом. `Koera Univrsity` проверяет исправление опечатки, `Cambridge` — выбор из вариантов, выдуманное название — отсутствие результатов.
5. `/profile` без ID и `/compare` — явно помеченные демонстрационные экраны. Реальные профили открываются из поиска с `?id=Q…`. Демонстрационные данные не используются для результатов поиска.

## Запуск развернутой версии

- Backend: задать реальные ключи только в `backend/.env`, `FRONTEND_URL` — точный HTTPS origin сайта. Предварительно скачать веса OpenCLIP командой выше. Запустить `uvicorn app.main:app --host 0.0.0.0 --port 8000` из `backend/` за HTTPS-прокси.
- Frontend: до сборки задать `NEXT_PUBLIC_API_URL` с публичным HTTPS URL backend, затем `npm ci && npm run build && npm run start`. На сайте для жюри нельзя оставлять `localhost:8000`.
- Прокси должен пропускать поток SSE без буферизации и иметь таймаут более 45 секунд. `/health` должен отвечать `{"status":"ok"}`.
- После изменения backend-ключей перезапустить процесс: настройки читаются при запуске. Изменение `NEXT_PUBLIC_API_URL` требует новой frontend-сборки.
- Передавать исходники через GitHub; локальные `.env`, `.venv`, `node_modules`, `.next` и файлы учета расходов в архив не включать.
