# Visual Campus — frontend

Next.js 16 (App Router) + TypeScript + Tailwind CSS v4.

```bash
npm install
cp .env.local.example .env.local   # NEXT_PUBLIC_API_URL=http://localhost:8000
npm run dev
```

See the root [README](../README.md) for the project overview, structure and limitations.
